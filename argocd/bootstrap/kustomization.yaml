apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - _cilium.yaml
  - _coredns.yaml
  - _argocd-config.yaml
  - _infra-apps.yaml

sortOptions:
  order: fifo
